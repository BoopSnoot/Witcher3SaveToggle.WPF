<Window x:Class="Witcher3SaveToggle.AddUserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:Witcher3SaveToggle"
        mc:Ignorable="d"
        Title="Add user" SizeToContent="WidthAndHeight" MinWidth="200" WindowStyle="ToolWindow">
    <Window.Resources>
    </Window.Resources>
    <StackPanel Margin="10">
        <Label>User codename:</Label>
        <TextBox x:Name="CodeNameTextBox"/>
        <Label>User display name:</Label>
        <TextBox x:Name="DisplayNameTextBox"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Style="{StaticResource Button}">Cancel</Button>
            <Button Style="{StaticResource Button}">Save</Button>
        </StackPanel>
    </StackPanel>
</Window>
